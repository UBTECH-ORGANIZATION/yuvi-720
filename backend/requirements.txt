fastapi
uvicorn
websockets
python-multipart
httpx
mcp>=1.28,<2
agent-framework-core==1.11.0
motor
certifi
tzdata
defusedxml>=0.7,<1
PyJWT>=2.9.0,<3.0
azure-communication-email
azure-storage-blob>=12.20,<13
aiohttp>=3.10,<4
azure-identity>=1.17,<2
azure-monitor-opentelemetry>=1.6,<2
opentelemetry-instrumentation-fastapi
# Outbound dependency tracking. The Azure Monitor distro auto-instruments only
# requests/urllib/flask/django/psycopg2 — but our latency lives in httpx (APIM,
# Kata, LRS), pymongo (every Cosmos query, via motor) and aiohttp. Without these
# App Insights shows a slow request and no reason for it.
opentelemetry-instrumentation-httpx
opentelemetry-instrumentation-pymongo
opentelemetry-instrumentation-aiohttp-client
manim>=0.19,<0.20
importlib-metadata>=4.6; python_version < "3.10"

# Chemistry visuals: validates model-emitted SMILES before anything is
# rendered, and computes formula/mass. See app/agents/molecule.py.
rdkit>=2024.3
