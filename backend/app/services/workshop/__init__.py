"""Yuvi Workshop — the learner-facing creation surface.

A learner describes something in their own language, an agentic builder writes a
single self-contained HTML document, and the result is stored as an immutable
version in a private blob container and rendered in an opaque-origin sandbox.
"""
