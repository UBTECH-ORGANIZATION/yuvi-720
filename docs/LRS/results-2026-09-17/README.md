# תוצאות תסריט הבדיקות xAPI — 17/09/2026

הקבצים כאן ממלאים את `../test script xAPI.csv` (התסריט של המשרד) מתוך ראיות בלבד: כל שורה
מסווגת לפי ההצהרות שנרשמו ב-`lrs_outbox` בחלון הריצה ונקלטו ב-LRS הבדיקות
(`lrs-stg.education.gov.il`). כלים: `frontend/scripts/lrs-test-script-run.mjs` (מריץ את התסריט
דרך הממשק), `backend/scripts/lrs_ledger.py --validate` (קורא את הפנקס ומאמת מול מפרט 1.1),
`backend/scripts/lrs_test_script_report.py` (ממלא את ה-CSV).

| קובץ | מה זה |
|---|---|
| `test-script-xapi-results-2026-09-17-dev.csv` | **הקובץ למשרד** — הריצה מול `dev.spark.yuvilab.ai` (הקוד הפרוס, sha `157d7ae8650b`), 340 הצהרות, 52 עבר / 0 לא עבר / 2 לא נבדק / 8 לא רלוונטי |
| `lrs-contract-report-2026-09-17-dev.csv` | אינדקס החוזה (סוג אובייקט × פועל) עם מזהה הצהרה אמיתי לכל צירוף, מריצת dev |
| `tc-map-dev.json`, `summary-dev.md` | מיפוי TC → מזהי הצהרות, זמנים, סטטוס משלוח, הערות |
| `…-local.*` | ריצת ה-localhost המוקדמת (125 הצהרות) — ראיית ביניים בלבד; בוצעה לפני שלבי התוכן ולפני חלק מהתיקונים, ולכן "לא עבר" בה אינו מצב הקוד הנוכחי |

שתי שורות "לא נבדק" (TC-ITM-10/11) דורשות השלמת רכיב הערכה (`isAssessment=true`) פעם
בהצלחה ופעם בכישלון; ביחידות methodica אלו רכיבי "שאלת שיא" (`…-01-05`, `…-02-05`, `…-02-06`).
שורה 25 (דילוג על רכיב) מסומנת "לא רלוונטי" — הכפתור "אני כבר יודע/ת" הוסר והאירוע אינו נתמך.
